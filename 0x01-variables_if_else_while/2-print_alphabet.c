#include <stdio.h>

/**
 * main - Entry point
 * Description: prints all alphabet in lower case
 * Return: 0
*/

int main(void)
{
char ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch++;
}
return (0); }
