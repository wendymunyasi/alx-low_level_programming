#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 * Description: prints all alphabet in lower case
 * Return: 0
*/

int main(void)
{
int i;

for (i = 0; i <= 9; i++)
printf("%i", i);
printf("\n");
return (0); }
