#include "main.h"

/**
 * mul - Function that multiplies two integers.
 * @a: first number.
 * @b: second number.
 *
 * Return: the result of multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
